import { ShortenTokenPipe } from './shorten-token.pipe';

describe('ShortenTokenPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortenTokenPipe();
    expect(pipe).toBeTruthy();
  });
});
